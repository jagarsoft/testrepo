build:
    nodes:
        environment:
            php:
                version: '>=8.0'
    coverage:
        tests:
            override:
                - command: XDEBUG_MODE=coverage vendor/bin/phpunit --testdox --verbose
                  coverage:
                    file: coverage-report/coverage.xml
                    format: clover
